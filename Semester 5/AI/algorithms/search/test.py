tree = {
        'a': ['b', 'c'],
        'b': ['c', 'e'],
        'c': ['d'],
        'd': [],
        'e': []
    }

def dfs(graph, start, limit=float('inf')):
    visited = []
    if start not in visited:
        print(start, end=', ')
    for


if __name__ == '__main__':
    dfs()
